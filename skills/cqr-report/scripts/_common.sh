#!/bin/bash
# Source the shared CQR helpers from cqr-standards
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
COMMON_PATH="$SCRIPT_DIR/../../cqr-standards/scripts/_common.sh"
source "$COMMON_PATH"

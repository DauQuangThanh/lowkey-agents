#!/bin/bash
# Shared helpers for Scrum Master skills — symlink to sprint-planning version
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
source "$SCRIPT_DIR/../../sm-sprint-planning/scripts/_common.sh"
